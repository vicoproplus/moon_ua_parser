# Evidence — T-03 regeneration idempotency verification (snapshot-maintain mode)

Date: 2026-09-13 · Branch: `v02-rules-update` · Worktree HEAD at start: `f2f933d`
Mode: decision gate T-01 ruled "upstream has no new commit" → snapshot-maintain:
regeneration must reproduce the committed generated files content-identically
(same input ⇒ same output). Nothing gets bumped; README is verified, not updated.

Toolchain: moon 0.1.20260904 (94521db 2026-09-04) · Python 3.12.10 · PyYAML 6.0.2.
`scripts/gen_rules.py` and `scripts/gen_tests.py`: untouched throughout
(`git status --porcelain -- scripts/` empty before and after).

## R1 — Pre-regeneration backup (system TEMP, outside the repository)

Backup path: `F:/Temp/moon_ua_t03_regen_20260913/`

```
cp -r moon_ua_parser_lib/src/ua_parser/rules   F:/Temp/moon_ua_t03_regen_20260913/rules.bak-20260913
cp -r moon_ua_parser_lib/tests/differential    F:/Temp/moon_ua_t03_regen_20260913/differential.bak-20260913
```

- `rules.bak-20260913`: 5 files (moon.pkg, rules_data.mbt, rules_init.mbt,
  rules_init_test.mbt, rules_version.mbt)
- `differential.bak-20260913`: 4 files (diff_ua.mbt, diff_os.mbt, diff_device.mbt, moon.pkg)
- MD5 checksums recorded at `F:/Temp/moon_ua_t03_regen_20260913/backup-md5.txt`, e.g.
  `rules_data.mbt` = `6af15703682eae56ac0740162ec8dfd7`,
  `diff_device.mbt` = `4e00f963869283cd59f2e2eff28703c3`.
  (Note: backup copies carry the worktree's CRLF checkout; regenerated files are
  LF — see EOL note below. Content comparison is therefore done with
  `diff -r --strip-trailing-cr` and via git blob ids, not raw `diff -r`.)

## R2 — Regeneration runs (CWD = `moon_ua_parser_lib/`)

Commands (same shape as `.github/workflows/ci.yml` "Generation consistency" step):

```
python ../scripts/gen_rules.py    # exit 0
python ../scripts/gen_tests.py    # exit 0
```

`gen_rules.py` stdout (identical on run 2):

```
Reading F:\moonbit比赛\moon_ua_parser-sdd\uap-core\regexes.yaml
uap-core snapshot: commit 73e7340, date 2026-08-24
user_agent_parsers  :  433 rules (0 with flag i)
os_parsers          :  204 rules (0 with flag i)
device_parsers      :  633 rules (65 with flag i)
total               :  1270 rules
wrote ...\src\ua_parser\rules\rules_data.mbt (257923 bytes)
wrote ...\src\ua_parser\rules\rules_version.mbt (465 bytes)
wrote ...\src\ua_parser\rules\moon.pkg (378 bytes)
OK
```

`gen_tests.py` stdout (identical on run 2):

```
Reading F:\moonbit比赛\moon_ua_parser-sdd\uap-core\tests
test_ua.yaml      :   1601 cases (0 exempted)
test_os.yaml      :    483 cases (0 exempted)
test_device.yaml  :  16129 cases (0 exempted)
total             :  18213 cases
wrote ...\tests\differential\diff_ua.mbt (342278 bytes)
wrote ...\tests\differential\diff_os.mbt (103272 bytes)
wrote ...\tests\differential\diff_device.mbt (3542364 bytes)
wrote ...\tests\differential\moon.pkg (305 bytes)
OK
```

Both counts match the committed baseline: 1270 rules (433/204/633, 65 flag-i)
and 18213 differential cases (1601/483/16129). Double-check (double insurance):
generators were run a second time — stdout byte-identical across runs, exit 0 both.

## R3 — Content-level idempotency assertions (CWD = `moon_ua_parser_lib/`)

Gate (the authoritative assertion):

```
$ git diff --exit-code -- src/ua_parser/rules tests/differential
(exit code 0, no output lines — only git's LF/CRLF warnings on stderr)
```

Result: **exit 0 on run 1 and on the double-check run 2**. Zero content diff.

Porcelain output, recorded as required (M flags present and allowed):

```
$ git status --porcelain
 M moon_ua_parser_lib/src/ua_parser/rules/moon.pkg
 M moon_ua_parser_lib/src/ua_parser/rules/rules_data.mbt
 M moon_ua_parser_lib/src/ua_parser/rules/rules_version.mbt
 M moon_ua_parser_lib/tests/differential/diff_device.mbt
 M moon_ua_parser_lib/tests/differential/diff_os.mbt
 M moon_ua_parser_lib/tests/differential/diff_ua.mbt
 M moon_ua_parser_lib/tests/differential/moon.pkg
```

EOL mechanism (as adjudicated in the task brief): repo has `core.autocrlf=true`,
no `.gitattributes`; HEAD blobs are LF, worktree was checked out CRLF, the
generators write LF (`newline="\n"`). The M flags are conversion-safety
dirtying only. Confirmed two independent ways:

1. `diff -r --strip-trailing-cr src/ua_parser/rules <backup>` and
   `diff -r --strip-trailing-cr tests/differential <backup>` → identical
   (raw `diff -r` differs only on line endings, as expected).
2. Git blob-id comparison of every generated file against the index — 9/9 MATCH:
   ```
   MATCH src/ua_parser/rules/moon.pkg            05a353bca3218bce68efafcb9f7b3e00bb2707e2
   MATCH src/ua_parser/rules/rules_data.mbt      0b2c42758fd6195dc44592192664747a34cbacf3
   MATCH src/ua_parser/rules/rules_version.mbt   171d582f25ba3e0cfb034b2d88c66cf9233a737a
   MATCH src/ua_parser/rules/rules_init.mbt      34624a6a789f374542c3c6eb56a7f3c8b20def82
   MATCH src/ua_parser/rules/rules_init_test.mbt 643ff1e4bf0442a0901d61a0efedd82bfe3ddb9b
   MATCH tests/differential/diff_ua.mbt          7dab2c043aedd5a438aca04b12d2581ad03e0aff
   MATCH tests/differential/diff_os.mbt          74ee8aa1b00dcf31ae3dc6adbb43641204602c06
   MATCH tests/differential/diff_device.mbt      8b9e7c6642617aada002b056b5d74b6d9d7d383e
   MATCH tests/differential/moon.pkg             fa46f3ad9830cd3650679f1fc3785c6652dd605c
   ```

No `.gitattributes` added, `core.autocrlf` not changed, no hand edits to any
generated file (EOL SOP is T-05's responsibility).

## R4 — moon check / moon info (public API zero drift)

```
$ moon check        → Finished. moon: ran 21 tasks ... (44 warnings, 0 errors); exit 0
$ moon info         → Finished. moon: ran 12 tasks ... (44 warnings, 0 errors); exit 0
$ git diff --exit-code -- '*.mbti' '**/*.mbti'   (from moon_ua_parser_lib/)
                    → exit code 0 — public API zero drift (pkg.generated.mbti unchanged)
```

The 44 warnings are pre-existing deprecation warnings in hand-written test
files (`tests/semantics`, `derive(Show)` etc.), not in generated files.

Housekeeping: `moon info` additionally materialized 7 untracked
`pkg.generated.mbti` files in packages that have never tracked one (root,
examples/middleware, src/ua_parser/rules, tests/differential, tests/diffstats,
tests/robust, tests/semantics). These were byproducts of the verification run,
did not exist at task start, are not part of the repo's tracked interface set
(HEAD tracks only `src/ua_parser/pkg.generated.mbti`), and were deleted after
the mbti gate was recorded. After deletion the mbti gate was re-run: still exit 0.

## R5 — README snapshot provenance verification (no fix needed)

Cross-checked every claim in `moon_ua_parser_lib/README.mbt.md` "Snapshot
provenance" (and the header/quality sections) against generator stdout and the
regenerated tree:

| README claim | Actual (this run) | Verdict |
|---|---|---|
| uap-core commit `73e7340` | gen_rules stdout + `rules_version.mbt` constant | match |
| snapshot date 2026-08-24 | gen_rules stdout + `rules_version.mbt` constant | match |
| `regexes.yaml`: 1270 rules | generator stdout: total 1270 | match |
| 433 UA / 204 OS / 633 device, 65 case-insensitive device | generator stdout (65 flag-i in device domain) | match |
| 18213 differential cases | generator stdout: total 18213 | match |
| per-domain 1601 / 483 / 16129 | generator stdout + diffstats run below | match |
| "regenerate byte-identically" | git diff exit 0 (R3) | match |

No "upstream latest"/"after upgrade" wording present (grep: none). README
needed **no changes**. Mechanism check: `scripts/gen_rules.py` lines 65–66 pin
`UAP_CORE_COMMIT = "73e7340"` / `UAP_CORE_DATE = "2026-08-24"`, which flow into
`rules_version.mbt` — post-regeneration file re-read confirms both constants
unchanged, and its blob id equals the committed one (see R3 table).

## R9 support — differential gates re-measured on regenerated suite

```
$ moon run --target native --release tests/diffstats
domain   total        exempted  passed  failed  rate      gate       status
browser  total=1601   0         1601    0       100.00%   gate>=99%  MET
os       total=483    0         483     0       100.00%   gate>=97%  MET
device   total=16129  0         16129   0       100.00%   gate>=97%  MET
GATES: ALL MET
```

`patch_minor` expectation-column exclusion policy unchanged (assertion logic in
the differential suite untouched; suite content blob-identical to HEAD).

## Outcome

Zero-diff maintenance verification. No generated file changed at content level;
README verified correct with no edits. The only repository change in this task
is this evidence file. No commit of generated files is needed or made — their
worktree M flags are EOL dirtying only and disappear under git's content
comparison (`git diff` exit 0), which is the gate that matters.
