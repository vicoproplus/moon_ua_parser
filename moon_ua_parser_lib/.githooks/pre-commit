#!/bin/sh

# moon.mod lives in the moon_ua_parser_lib/ subdirectory, while git runs
# hooks from the repository root — cd there before invoking the toolchain.
cd "$(git rev-parse --show-toplevel)/moon_ua_parser_lib" || exit 1

moon check
